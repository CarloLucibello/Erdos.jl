coverage:
  status:
    patch:
      default:
        target: '95'
    project:
      default:
        target: '95'
comment:
  layout: "header, diff"
  behavior: default
  require_changes: false  # if true: only post the comment if coverage changes
  branches: null
  flags: null
  paths: null
